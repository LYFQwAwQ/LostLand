# AGENTS.md
## 1. 目标与范围
- 项目：`LostLand`（React + TypeScript + Vite）

## 2. 文档顺序

1. `docs/开发文档/00-开发主指导.md`
2. `docs/开发文档/README.md`

## 3. 文档更新要求

每次开发完成后，必须同步更新文档：

1. 至少更新一个对应模块文档（说明改动点与影响面）。
2. 新增一份开发记录：
   - 路径：`docs/开发文档/开发记录/YYYY-MM-DD-任务名.md`
   - 模板：`docs/开发文档/开发记录/开发记录模板.md`
3. 若新增文档或新增模块，必须更新：
   - `docs/开发文档/README.md` 索引


3. 有新的可调数值参数要写在docs/开发文档/配置文件修改指南.md中

## 4. 工程边界与禁止项

- 非明确需求，不改以下工程配置：
  - `package.json`
  - `vite.config.ts`
  - `tsconfig.json`
  - `tsconfig.node.json`
- 不做与任务无关的大规模重构。
- 不删除或覆盖已有文档规范，除非用户明确要求。

## 5. Memory 维护规则（Codex）

当出现“长期有效的新约定”时，直接更新本文件；临时任务信息不要写入本文件。  
可写入的长期信息包括：

- 团队统一流程变更
- 文档路径和命名规范变更

## 6. TODO 维护规范

- 项目存在统一待办文件：`docs/开发文档/TODO-项目后续开发.md`。
- 发现“临时实现 / 暂未开发 / 明确后续要补”的内容时，开发完成后需要把待办项补充到该文件，并在TODO中将本次开发已经完成的内容删除，补充代办事项时，不应太简洁，尽量将什么系统/界面/功能等要添加什么，可能可以从什么文件入手写出。
- 不再新增模块私有 TODO 文件，统一收敛到上述项目 TODO，便于后续开发者检索与排期。
