# 数值配置修改指南（策划版）

这份文档只讲“数值怎么改”，不涉及工程代码和构建配置。

## 1. 快捷导航（先看这里）

## 1.1 按目标快速定位

- 想减缓混沌扩张
  - 改 `src/lib/archetypes.ts`：`BL1/BL2/BL3.expansionScale`、`aggressionScale`
  - 改 `src/lib/monthlySimulation.ts`：`EXPANSION_DIFF_FACTOR`、`BL* 的 negativeMonths 阈值`
- 想加快地图演化节奏
  - 改 `src/lib/monthlySimulation.ts`：把 `>= 6/4/3/2` 类阈值调小
- 想让地图更密/更稀
  - 改 `src/data/mockData.ts`：`basePointCount`、`minDistance`、`complexity`
- 想扩大/缩小场影响范围
  - 改 `src/data/mockData.ts`：`maxRadius`
- 想调经济（价格/库存/打造费）
  - 改 `src/data/nodeModules.ts`：`price`、`stock`、`signingCost`、`goldCost` 和倍率系数
- 想调装备掉落与词条强度
  - 改 `src/data/equipmentTemplates.ts`：模板权重、基础属性、词条池范围与权重
  - 改 `src/lib/equipmentSystem.ts`：品阶增幅区间、品质词条数量、插槽解锁间隔
- 想调背包装备卡片颜色分档
  - 改 `src/lib/equipmentScoring.ts`：内部评分权重与阈值分段
- 想调战斗节奏/技能强度/敌人规模
  - 改 `src/lib/battleEngine.ts`：行动阈值、Tick 系数、防御常数、单 Tick 行动上限
  - 改 `src/data/battleSkills.ts`：技能倍率、MP/CD、状态概率、权重修正
  - 改 `src/data/battleUnits.ts`：敌人数、敌人等级成长、敌人基础面板
  - 改 `src/data/battleDrops.ts`：怪物掉落表、材料/装备掉落次数、装备稀有度权重

## 1.2 按关键词快速查找（Ctrl+F）

- `initialStrength`：节点基础强度
- `orderScale`：秩序场强度
- `expansionScale`：扩张场强度
- `aggressionScale`：扩张进攻倾向
- `negativeMonths >=`：连续负向月触发条件
- `highProsperityMonths >=`：连续高繁荣触发条件
- `basePointCount`：节点总数
- `maxRadius`：影响传播半径
- `price * (1 +`：价格动态公式
- `EQUIPMENT_RANK_PERCENT_RANGES`：品阶增幅范围
- `EQUIPMENT_QUALITY_AFFIX_COUNT`：品质词条数映射
- `SOCKET_UNLOCK_LEVEL_STEP`：插槽等级步进
- `baseWeight/rankWeights/qualityWeights`：装备与稀有度权重
- `EQUIPMENT_SCORE_THRESHOLDS`：背包装备颜色分档阈值
- `EQUIPMENT_QUALITY_SCORE/EQUIPMENT_RANK_SCORE`：品质/品阶内部评分权重
- `ACTION_SPEED_COEFFICIENT`：行动条推进速度
- `DEFENSE_K`：防御区系数常量
- `MAX_ACTIONS_PER_STEP`：单 Tick 最多处理行动数
- `basePower/scaling/mpCost/cooldown/baseWeight`：技能核心参数
- `resolveEnemyCount`：敌人数生成规则
- `levelBonusByArchetype`：BL 分档等级补正
- `materialRolls/equipmentRolls`：每个怪物的材料/装备掉落次数区间
- `templateIds/qualityWeightMultipliers`：装备掉落模板池与稀有度权重

## 1.3 你只需要改这些文件

1. `src/lib/archetypes.ts`
2. `src/lib/monthlySimulation.ts`
3. `src/data/mockData.ts`
4. `src/data/nodeModules.ts`
5. `src/lib/equipmentSystem.ts`
6. `src/data/equipmentTemplates.ts`
7. `src/lib/equipmentScoring.ts`
8. `src/lib/battleEngine.ts`
9. `src/data/battleSkills.ts`
10. `src/data/battleUnits.ts`
11. `src/data/battleDrops.ts`

## 2. 参数词典：`src/lib/archetypes.ts`

位置：`ARCHETYPE_PROFILES`

- `initialStrength`
  - 含义：该类型节点的基础强度底盘。
  - 调大：该类型更容易“压住局势”。
  - 调小：该类型更容易被对抗、被转化。
- `orderScale`
  - 含义：节点向外输出秩序场的系数（稳定倾向）。
  - 调大：更容易提升稳定、抑制混乱。
  - 调小：稳定扩散能力下降。
- `expansionScale`
  - 含义：节点向外输出扩张场的系数（混乱倾向）。
  - 调大：更容易扩张、侵蚀周边。
  - 调小：混乱传播放缓。
- `aggressionScale`
  - 含义：扩张进攻系数，影响扩张贡献和成长压力。
  - 调大：扩张推进更强、更快。
  - 调小：扩张冲击更弱。
- `stableTier` / `chaosTier`
  - 含义：层级标记（主要用于逻辑分类）。
  - 建议：策划一般不改。
- `defaultDifficulty`
  - 含义：UI 展示难度文案。
  - 建议：只影响显示，不直接改变结算强度。

## 3. 参数词典：`src/lib/monthlySimulation.ts`

## 3.1 全局对抗系数

- `ORDER_SAME_FACTOR = 1.2`
  - 含义：同阵营秩序加成倍率。
  - 调大：同阵营抱团稳定更明显。
- `ORDER_DIFF_FACTOR = 0.7`
  - 含义：异阵营秩序作用倍率。
  - 调大：跨阵营秩序影响更强。
- `EXPANSION_DIFF_FACTOR = 1.5`
  - 含义：异阵营扩张倍率。
  - 调大：跨阵营扩张更凶。
- `EXPANSION_SAME_FACTOR = 0.7`
  - 含义：同阵营扩张倍率。
  - 调大：同阵营内部扩张干扰增强。

## 3.2 关键触发阈值（节奏核心）

- `developmentMonths >= 4`
  - 含义：未激活点连续正向发展多少月后可开发。
  - 调大：新点诞生更慢。
  - 调小：新点诞生更快。
- `positiveMonths >= 2`（幽灵位重建）
  - 调大：幽灵位回归更慢。
  - 调小：幽灵位回归更快。
- `NOD negativeMonths >= 2`（资源点坍塌）
  - 调大：资源点更耐打。
  - 调小：资源点更脆弱。
- `ST1 negativeMonths >= 6`（主城混沌化）
  - 调大：主城更难失守。
  - 调小：主城更易失守。
- `ST2/ST3 negativeMonths >= 2`（稳定节点降级/转化）
  - 调大：稳定链更稳。
  - 调小：稳定链更易崩。
- `BL1 negativeMonths >= 2`（灾厄扩散）
  - 调大：灾厄扩散变慢。
  - 调小：灾厄扩散变快。
- `BL3/BL2 negativeMonths >= 3`（混沌升级）
  - 调大：混沌升级慢。
  - 调小：混沌升级快。
- `highProsperityMonths >= 2/4/8`
  - 含义：繁荣连续月触发升级或净化。
  - 调大：净化和正向升级更慢。
  - 调小：净化和正向升级更快。

## 3.3 其他常用调节点

- `penalty = (totalOrder + totalExpansion) * 0.1`
  - 含义：高对抗时的繁荣惩罚/补偿系数。
  - 调大：高强度对抗带来的波动更大。
- `prosperity clamp(-1800, initialStrength * 20)`
  - 含义：繁荣上下限。
  - 调大上限：高强度节点能积累更高繁荣。

## 4. 参数词典：`src/data/mockData.ts`

位置：`createInitialRegions() -> buildRegionTopology({...})`

- `seed`
  - 含义：地图随机种子（同 seed 可复现）。
  - 改动：地图布局会变化。
- `basePointCount`
  - 含义：节点数量。
  - 调大：图更大、更复杂。
  - 调小：图更小、更清晰。
- `minDistance`
  - 含义：节点最小间距。
  - 调大：更稀疏。
  - 调小：更密集。
- `complexity`
  - 含义：连线保留复杂度。
  - 调大：更多支路和环路。
  - 调小：更偏主干路线。
- `maxRadius`
  - 含义：场传播最大半径。
  - 调大：远距离相互影响增强，局势变化更快。
  - 调小：本地化更强，局势变化更慢。

## 5. 参数词典：`src/data/nodeModules.ts`

## 5.1 静态池参数

- 商店/市场条目：
  - `price`：基础价格
  - `stock`：基础库存
  - `weight`：展示权重字段（当前主要用于显示）
- 酒馆条目：
  - `signingCost`：招募费用
- 铁匠条目：
  - `goldCost`：打造金币消耗

## 5.2 动态系数（最常调）

- 商店价格：`item.price * (1 + index * 0.04)`
  - `0.04` 调大：后排商品更贵。
- 市场价格：`item.price * (1 + (index % 3) * 0.03)`
  - `0.03` 调大：材料价差更大。
- 市场库存：`item.stock + (positiveMonths > 0 ? 8 : 0) - index * 3`
  - `8` 调大：繁荣后供给提升更多。
  - `3` 调大：后排库存衰减更快。
- 酒馆费用：`signingCost * (1 + max(prosperity, 0) / 3000)`
  - `3000` 调小：繁荣对招募涨价更敏感。
- 打造费用：`goldCost * (1 + index * 0.08)`
  - `0.08` 调大：高位配方涨价更明显。

## 6. 推荐改数值流程（避免改乱）

1. 一次只改一个目标（例如“只减缓混沌扩张”）。
2. 一次只改 1-3 个参数。
3. 连续结算 5-10 个月观察结果。
4. 记录“改了什么 -> 结果如何”，再进下一轮。

## 7. 快速验证清单

1. 地图可正常打开。
2. “结算下一个游戏月”可连续点击且不报错。
3. 月事件和节点变化符合你的调参目标。
4. 经济页（商店/市场/酒馆/铁匠）价格或库存变化符合预期。

## 8. 不建议策划修改的文件

- `package.json`
- `vite.config.ts`
- `tsconfig.json`
- `tsconfig.node.json`
- `index.html`

## 9. 参数词典：装备系统（新增）

### 9.1 `src/lib/equipmentSystem.ts`

- `EQUIPMENT_RANK_PERCENT_RANGES`
  - 含义：品阶增幅区间（劣质/精良/卓越/完美）。
  - 调大：同品阶下的属性上限更高。
  - 调小：同品阶下的属性波动更保守。
- `EQUIPMENT_QUALITY_AFFIX_COUNT`
  - 含义：品质与词条条数的映射关系（白0到红5）。
  - 调大：高品质装备词条数量更激进。
  - 调小：装备上限下降、词条密度降低。
- `SOCKET_UNLOCK_LEVEL_STEP`
  - 含义：每多少级解锁一个插槽（当前 `20`）。
  - 调大：插槽成长更慢。
  - 调小：插槽成长更快。
- `DEFAULT_EQUIPMENT_ENVIRONMENT`
  - 含义：环境权重修正默认值（当前全部 `1`，即 `M=1.0`）。
  - 用途：后续可用于地图压制或活动对 Rank/Quality 掉率做动态修正。

### 9.2 `src/data/equipmentTemplates.ts`

- `baseWeight`
  - 含义：模板在掉落池中的抽取权重。
  - 调大：该装备模板更容易出现。
- `t1Stats[].lvl1Base / growthRate`
  - 含义：基础属性 1 级底值与每级线性成长。
  - 调大：该属性“地板”更高。
- `rankWeights`
  - 含义：该模板生成不同品阶的概率权重。
  - 调大某档：更容易出现该品阶。
- `qualityWeights`
  - 含义：该模板生成不同品质（词条数）的概率权重。
  - 调大高品质：高词条数装备出现更多。
- `affixPool[].min / max / weight`
  - 含义：词条数值范围与词条出现权重。
  - 调大 `weight`：该词条更常见。
  - 提高 `min/max`：该词条强度整体上移。
- `t1Stats[].key / affixPool[].key`
  - 约束：当前仅允许使用《设定/属性》文档中的属性键，不新增自定义键。
  - 允许键：`hp`、`mp`、`str`、`int`、`agi`、`def`、`penetration`、`critRate`、`critDamage`、`evasion`、`aggro`、`lifeSteal`、`thorns`、`elementalPierce`、`allRes`、`allBoost`、`armorPiercePct`。

### 9.3 `src/lib/equipmentScoring.ts`

- `EQUIPMENT_QUALITY_SCORE`
  - 含义：背包内部分数中，各品质的基础权重。
  - 调大高品质权重：高品质装备更容易进入高色阶。
- `EQUIPMENT_RANK_SCORE`
  - 含义：背包内部分数中，各品阶的基础权重。
  - 调大高品阶权重：高品阶装备更容易获得更高视觉档位。
- `EQUIPMENT_SCORE_THRESHOLDS`
  - 含义：内部总分对应的颜色分档阈值（低/精良/稀有/史诗/传说/神话）。
  - 调小阈值：整体颜色更“偏高级”。
  - 调大阈值：整体颜色更“偏保守”。

## 10. 参数词典：战斗系统（新增）

### 10.1 `src/lib/battleEngine.ts`

- `ACTION_THRESHOLD = 10000`
  - 含义：行动条触发阈值。
  - 调大：单位行动更慢，单场战斗时长增加。
  - 调小：单位行动更频繁。
- `ACTION_SPEED_COEFFICIENT`
  - 含义：AGI 转换为行动条推进速度的系数。
  - 调大：整体战斗加速。
  - 调小：整体战斗减速。
- `DEFENSE_K`
  - 含义：防御减算区常量（`K / (K + EffectiveDEF)`）。
  - 调大：防御影响减弱，伤害更高。
  - 调小：防御影响增强。
- `MAX_ACTIONS_PER_STEP`
  - 含义：单次逻辑 Tick 最多处理的行动次数。
  - 调大：高 AGI 场景更顺滑，但日志增长更快。
  - 调小：节流更强，可能出现行动堆积。
- `LOG_LIMIT`
  - 含义：日志列表最大缓存长度。
  - 调大：保留更多历史；调小：内存与渲染压力更小。

### 10.1.1 `src/pages/BattlePage.tsx`

- 自动连战衔接延迟（`setTimeout(..., 900)`）
  - 含义：每场结束后进入下一场前的等待时间（毫秒）。
  - 调大：战斗结束停顿更久，更易读日志。
  - 调小：衔接更快，更接近挂机节奏。

### 10.2 `src/data/battleSkills.ts`

- `basePower`
  - 含义：技能基础数值（进入乘区前的基数）。
- `scaling`
  - 含义：技能属性系数（`str/int/def/maxHp/missingHp`）。
- `mpCost`
  - 含义：施放消耗 MP。
- `cooldown`
  - 含义：技能冷却回合数。
- `baseWeight`
  - 含义：动态权重的基础值。
- `weightTuning`
  - 含义：低血线、敌人数等环境权重修正。
- `targetStatus / selfStatus`
  - 含义：状态附加配置（概率、持续、强度）。

### 10.3 `src/data/battleUnits.ts`

- `resolveEnemyCount()`
  - 含义：按 BL 等级决定敌方人数区间（支持 1-6 结构）。
- `levelBonusByArchetype()`
  - 含义：BL 分档附加等级。
- `suppressionLevel`
  - 含义：地图压制转化的等级补正（影响敌方强度）。
- `hpScale / statScale`
  - 含义：敌方生命与主属性成长曲线。
- `ENEMY_POOL[].stats`
  - 含义：敌方模板基础面板与倾向。

### 10.4 `src/data/battleDrops.ts`

- `ENEMY_DROP_TABLES[enemyId].materialRolls`
  - 含义：该怪物每场掉落材料的抽取次数区间（闭区间）。
  - 调大：材料产出更高。
- `ENEMY_DROP_TABLES[enemyId].equipmentRolls`
  - 含义：该怪物每场掉落装备的抽取次数区间（闭区间）。
  - 调大：装备掉落更频繁。
- `materials[].weight / quantity`
  - 含义：材料池权重与单次掉落数量区间。
  - 调大权重：该材料更常掉；调大数量：每次掉更多。
- `equipments[].templateIds`
  - 含义：该装备掉落组可抽取的装备模板池（细粒度可按怪物区分）。
  - 调整建议：同一组内放同风格模板，保持掉落风味一致。
- `equipments[].environment.rankWeightMultipliers / qualityWeightMultipliers`
  - 含义：该怪物装备掉落的品阶/品质权重修正。
  - 调大高档位：更容易出高品阶、高品质装备。
- `equipments[].levelOffset`
  - 含义：掉落装备等级相对怪物等级的偏移。
  - 调大：装备等级更高；调小：更保守。
