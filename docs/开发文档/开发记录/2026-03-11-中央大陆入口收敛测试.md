# 开发记录

## 1. 任务信息

- 日期：2026-03-11
- 开发者：Codex
- 任务名称：中央大陆入口收敛测试
- 需求来源：用户要求“先测试中央大陆，其他大陆入口全部置灰禁用”

## 2. 改动目标

- 本次要解决什么问题：
  - 世界地图测试阶段只开放中央大陆入口，避免测试流量分散到其他大陆。
  - 即使手动修改 URL 到其他大陆，也自动回写到中央大陆选择，保证测试路径一致。
- 不在本次范围内的内容：
  - 不调整地区拓扑生成、月结算规则和节点演化逻辑。
  - 不改任务系统按疆域配置化（该项已暂缓）。

## 3. 代码改动清单

- 改动文件：
  - `src/pages/WorldMapPage.tsx`
  - `src/styles.css`
- 每个文件改了什么（1-2 句）：
  - `src/pages/WorldMapPage.tsx`：大陆按钮中仅 `central` 可点击；其他大陆禁用并置灰，同时将非中央大陆 URL 选择归一化为中央大陆。
  - `src/styles.css`：新增大陆按钮禁用态样式，确保“不可点”有明显视觉反馈。

## 4. 影响范围

- 影响页面/模块：
  - 世界地图页大陆选择入口
- 是否影响数值平衡：
  - 否（纯入口与交互限制）
- 是否影响旧功能：
  - 是（临时策略）：中央大陆以外入口不可进入，属于测试期行为收敛。

## 5. 验证记录

- 手动验证路径：
  - 打开世界地图，检查大陆按钮状态：中央大陆可点，其他大陆灰态不可点。
  - 手动改 URL 的 `continent/dominion/region` 为非中央大陆后刷新，确认会回写到中央大陆默认选择。
- 验证结果：
  - `npm run build` 通过（`tsc -b` + `vite build`）。
- 未覆盖风险：
  - 暂未覆盖浏览器历史回退场景下的多次 URL 归一化体验（功能正确但交互细节未专项测试）。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/02-地图生成与月度结算模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是（新增本开发记录索引）。
- 是否更新数值指南（如涉及）：
  - 否（未引入新数值参数）。

## 7. 回滚说明

- 快速回滚文件：
  - `src/pages/WorldMapPage.tsx`
  - `src/styles.css`
- 回滚后需要复查的点：
  - 其他大陆入口是否恢复可点击。
  - URL 非中央大陆选择是否不再被强制回写。
