# 组织扩张预览颜色被灰格覆盖修复

## 1. 任务信息

- 日期：2026-03-08
- 开发者：Codex
- 任务名称：扩张预览颜色覆盖修复
- 需求来源：用户反馈（扩张模式外圈移动时仍只显示灰色）

## 2. 改动目标

- 本次要解决什么问题：
  - 扩张模式下预览范围已计算，但被灰格样式覆盖，导致视觉上看不到扩张范围。
- 不在本次范围内的内容：
  - 扩张规则逻辑与悬停定位逻辑调整。

## 3. 代码改动清单

- 改动文件：
  - `src/styles.css`
- 每个文件改了什么（1-2 句）：
  - 新增 `.organization-cell.fogged.expand-mode.preview-ok/.preview-bad` 覆盖规则，提升扩张预览色在灰格上的优先级。

## 4. 影响范围

- 影响页面/模块：
  - `组织基地` 页面扩张模式视觉反馈。
- 是否影响数值平衡：
  - 否。
- 是否影响旧功能：
  - 否，仅修复样式覆盖关系。

## 5. 验证记录

- 手动验证路径：
  - 进入扩张模式，在领地外移动鼠标，观察预览范围是否显示为绿色/红色。
- 验证结果：
  - `npm run build` 通过（TypeScript + Vite 打包成功）。
- 未覆盖风险：
  - 无新增逻辑风险。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/06-UI与样式模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是。
- 是否更新数值指南（如涉及）：
  - 否。

## 7. 回滚说明

- 快速回滚文件：
  - `src/styles.css`
- 回滚后需要复查的点：
  - 扩张预览是否再次被灰格底色覆盖。
