# 组织扩张预览仅领地外显示

## 1. 任务信息

- 日期：2026-03-08
- 开发者：Codex
- 任务名称：扩张预览显示范围修正
- 需求来源：用户反馈（扩张预览不应在已有领地内显示）

## 2. 改动目标

- 本次要解决什么问题：
  - 扩张模式下鼠标位于已拥有地块时，错误显示了扩张预览范围。
- 不在本次范围内的内容：
  - 扩张规则本身（外放/相邻/不相交）与扩张范围大小。

## 3. 代码改动清单

- 改动文件：
  - `src/pages/OrganizationPage.tsx`
- 每个文件改了什么（1-2 句）：
  - 在扩张预览计算中增加“鼠标所在格是否属于已拥有领地”的判断；若在领地内则直接不生成扩张预览。

## 4. 影响范围

- 影响页面/模块：
  - `组织基地` 页面扩张模式悬停显示。
- 是否影响数值平衡：
  - 否。
- 是否影响旧功能：
  - 否，实际扩张校验与结果保持不变。

## 5. 验证记录

- 手动验证路径：
  - 进入扩张模式，在领地内移动鼠标，确认不显示扩张范围。
  - 在领地外移动鼠标，确认仍显示可/不可扩张范围预览。
- 验证结果：
  - `npm run build` 通过（TypeScript + Vite 打包成功）。
- 未覆盖风险：
  - 无新增逻辑风险，属于显示条件收敛。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/06-UI与样式模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是。
- 是否更新数值指南（如涉及）：
  - 否。

## 7. 回滚说明

- 快速回滚文件：
  - `src/pages/OrganizationPage.tsx`
- 回滚后需要复查的点：
  - 扩张模式在领地内是否恢复显示扩张预览（旧行为）。
