# 组织页右键快捷切换平移模式

## 1. 任务信息

- 日期：2026-03-07
- 开发者：Codex
- 任务名称：组织地图右键快捷切换模式
- 需求来源：用户需求（建造模式下右键快速切换到平移模式）

## 2. 改动目标

- 本次要解决什么问题：
  - 建造模式与平移模式切换依赖按钮点击，操作频率高时不够顺手。
- 不在本次范围内的内容：
  - 模式按钮布局与其他组织页功能逻辑。

## 3. 代码改动清单

- 改动文件：
  - `src/pages/OrganizationPage.tsx`
- 每个文件改了什么（1-2 句）：
  - 新增 `onViewportContextMenu` 处理器：在建造模式时右键地图直接切换为平移模式，并给出反馈文案。

## 4. 影响范围

- 影响页面/模块：
  - 组织基地页地图交互。
- 是否影响数值平衡：
  - 否。
- 是否影响旧功能：
  - 否，原有模式按钮与缩放/平移/建造逻辑保持可用。

## 5. 验证记录

- 手动验证路径：
  - 进入组织基地 -> 保持建造模式 -> 在地图区右键。
- 验证结果：
  - 成功切换到平移模式，且未弹浏览器默认右键菜单。
  - `npm run build` 通过。
- 未覆盖风险：
  - 当前仅支持“建造 -> 平移”单向快捷切换，未实现右键反向切回。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/06-UI与样式模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是。
- 是否更新数值指南（如涉及）：
  - 否。

## 7. 回滚说明

- 快速回滚文件：
  - `src/pages/OrganizationPage.tsx`
- 回滚后需要复查的点：
  - 右键是否恢复为仅禁用默认菜单（无快捷切换）。
