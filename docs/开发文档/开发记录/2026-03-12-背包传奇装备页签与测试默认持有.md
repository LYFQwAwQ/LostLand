# 开发记录：背包传奇装备页签与测试默认持有

## 1. 任务信息

- 日期：2026-03-12
- 开发者：Codex
- 任务名称：背包新增传奇装备界面 + 测试期默认持有全部传奇装备
- 需求来源：用户需求（本轮对话）

## 2. 改动目标

- 本次要解决什么问题：
  - 在背包页新增“传奇装备”页签，交互结构参照“记忆”页签。
  - 展示传奇装备基础信息、固定 5 符文槽、装备被动技能、当前装备归属。
  - 测试阶段默认拥有全部传奇装备，并保证英雄页穿戴校验与背包页一致。
  - 在 TODO 中登记“后续移除默认全拥有”工作。
- 不在本次范围内的内容：
  - 传奇装备正式掉落/解锁渠道接入。
  - 传奇装备战斗结算效果接入引擎。

## 3. 代码改动清单

- 改动文件：
  - `src/state/EquipmentInventoryProvider.tsx`
  - `src/pages/InventoryPage.tsx`
  - `src/styles.css`
- 每个文件改了什么（1-2 句）：
  - `src/state/EquipmentInventoryProvider.tsx`：新增 `ownedLegendaryEquipmentIds` 状态与快照字段，并在传奇装备穿戴时增加“必须已拥有”的校验；测试期默认回退为拥有全部传奇装备。
  - `src/pages/InventoryPage.tsx`：新增“传奇装备”页签（概览、关键词、排序、卡片列表），展示装备被动技能与装备归属。
  - `src/styles.css`：新增背包传奇装备网格与卡片样式，并补移动端单列适配。

## 4. 影响范围

- 影响页面/模块：
  - 背包页（`InventoryPage`）
  - 装备库存状态（`EquipmentInventoryProvider`）
  - 队伍预设快照（间接，因库存快照字段扩展）
- 是否影响数值平衡：
  - 否（展示与持有校验改动，不改战斗数值）
- 是否影响旧功能：
  - 否（装备/记忆/材料/消耗品原有逻辑保持）

## 5. 验证记录

- 手动验证路径：
  - 背包 -> 传奇装备页签：查看筛选、排序、条目展示与归属显示。
  - 英雄页 -> 装备体系：验证传奇装备穿戴仍正常，且受“已拥有”校验。
  - 执行 `npm run build` 做 TypeScript + 打包校验。
- 验证结果：
  - 构建通过；背包传奇装备页签可正常展示并与英雄页状态一致。
- 未覆盖风险：
  - 旧本地预设若带有未拥有传奇装备，需要后续接入正式获取后再做兼容回归验证。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/04-英雄与队伍模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是
- 是否更新数值指南（如涉及）：
  - 否（本次无新增数值配置项）

## 7. 回滚说明

- 快速回滚文件：
  - `src/state/EquipmentInventoryProvider.tsx`
  - `src/pages/InventoryPage.tsx`
  - `src/styles.css`
- 回滚后需要复查的点：
  - 背包页分页签是否恢复原先结构。
  - 英雄页传奇装备穿戴是否继续可用。
