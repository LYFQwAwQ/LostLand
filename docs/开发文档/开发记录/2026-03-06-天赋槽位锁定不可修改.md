# 2026-03-06-天赋槽位锁定不可修改

## 1. 任务信息

- 日期：2026-03-06
- 开发者：Codex
- 任务名称：天赋槽位锁定为不可修改
- 需求来源：用户需求（天赋不可修改）

## 2. 改动目标

- 本次要解决什么问题：
  - 英雄技能配置中的天赋槽位应为固定槽位，禁止手动调整。
  - 导入技能配置时不应覆盖当前英雄天赋。
- 不在本次范围内的内容：
  - 不改主动/被动技能配置逻辑。
  - 不改战斗引擎、元素触发或复盘逻辑。

## 3. 代码改动清单

- 改动文件：
  - `src/pages/HeroPage.tsx`
  - `src/styles.css`
- 每个文件改了什么（1-2 句）：
  - `HeroPage.tsx`：天赋下拉改为禁用态，并在应用导入配置时强制保留当前天赋值。
  - `styles.css`：补充天赋锁定提示文案样式与禁用下拉样式。

## 4. 影响范围

- 影响页面/模块：
  - 英雄页 `技能配置` 标签。
- 是否影响数值平衡：
  - 否。
- 是否影响旧功能：
  - 有，天赋从可编辑改为固定只读。

## 5. 验证记录

- 手动验证路径：
  - 英雄页 -> 技能配置：确认天赋下拉不可操作。
  - 导入配置：导入包含不同天赋的 JSON，确认当前天赋不被覆盖。
- 验证结果：
  - 天赋槽位已锁定，导入不会改天赋。
  - `npm run build` 通过。
- 未覆盖风险：
  - 未补自动化测试。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/04-英雄与队伍模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是（新增本开发记录索引）。
- 是否更新数值指南（如涉及）：
  - 否。

## 7. 回滚说明

- 快速回滚文件：
  - `src/pages/HeroPage.tsx`
  - `src/styles.css`
- 回滚后需要复查的点：
  - 天赋下拉是否恢复可编辑。
  - 导入配置是否重新允许覆盖天赋。
