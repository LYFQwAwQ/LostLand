# 2026-03-03-背包详情去框化与主信息融合

## 1. 任务信息

- 日期：2026-03-03
- 开发者：Codex
- 任务名称：背包详情去框化与主信息融合
- 需求来源：用户反馈（详情框体感过重、主信息需更贴近名称）

## 2. 改动目标

- 本次要解决什么问题：
  - 移除“品质/品阶/插槽区 + 基础属性区 + 附加词条区”的块状边框视觉。
  - 改为整体内容以虚线分隔，阅读更连续。
  - 将品质/品阶/插槽直接融合到装备名附近，不再单独列区。
  - 等级保持在标题行显示。
- 不在本次范围内的内容：
  - 不改动装备生成规则和数值逻辑。

## 3. 代码改动清单

- 改动文件：
  - `src/pages/InventoryPage.tsx`
  - `src/styles.css`
- 每个文件改了什么（1-2 句）：
  - `src/pages/InventoryPage.tsx`：删除独立元信息区，新增标题附近 `inventory-header-meta` 行承载品质/品阶/插槽。
  - `src/styles.css`：删除详情块边框样式，改为虚线分隔；新增 `inventory-header-meta` 样式并保留颜色标识。

## 4. 影响范围

- 影响页面/模块：
  - 背包页装备详情弹窗。
- 是否影响数值平衡：
  - 否（纯展示层改动）。
- 是否影响旧功能：
  - 否，点击查看详情与关闭交互不变。

## 5. 验证记录

- 手动验证路径：
  - 进入 `背包 -> 装备`，点击任一装备。
  - 检查：详情不再有多个方框分区；内容由虚线分隔；品质/品阶/插槽在标题附近显示。
- 验证结果：
  - 构建通过：`npm run build`。
  - 单测通过：`node --experimental-strip-types --test --test-isolation=none tests/equipmentSystem.test.ts`（7/7）。
- 未覆盖风险：
  - 后续若引入更多详情字段，需再评估单屏信息密度。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/06-UI与样式模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是
- 是否更新数值指南（如涉及）：
  - 否

## 7. 回滚说明

- 快速回滚文件：
  - `src/pages/InventoryPage.tsx`
  - `src/styles.css`
- 回滚后需要复查的点：
  - 详情结构是否恢复旧分区；
  - 标题附近元信息是否回退。
