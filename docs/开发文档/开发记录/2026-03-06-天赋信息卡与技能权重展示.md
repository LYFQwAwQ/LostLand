# 2026-03-06-天赋信息卡与技能权重展示

## 1. 任务信息

- 日期：2026-03-06
- 开发者：Codex
- 任务名称：技能页天赋展示改版与主动权重可视化
- 需求来源：用户需求（取消天赋下拉，展示天赋信息与稀有度；显示主动技能权重；提高三列可视高度）

## 2. 改动目标

- 本次要解决什么问题：
  - 天赋槽位不再显示下拉控件，改为固定信息卡展示。
  - 天赋信息增加稀有度分级与颜色标记（普通/稀有/史诗/传说/独特）。
  - 主动技能槽位在右侧展示当前技能权重（`baseWeight`）。
  - 技能三列高度上调，并移除槽位区内部滚动。
- 不在本次范围内的内容：
  - 不调整战斗公式与技能数值。
  - 不修改导入格式与复盘逻辑。

## 3. 代码改动清单

- 改动文件：
  - `src/types/battle.ts`
  - `src/data/battleSkills.ts`
  - `src/pages/HeroPage.tsx`
  - `src/styles.css`
- 每个文件改了什么（1-2 句）：
  - `types/battle.ts`：新增天赋稀有度类型 `BattleTalentRarity`，并在天赋定义中声明 `rarity`。
  - `data/battleSkills.ts`：为现有天赋补齐稀有度字段。
  - `HeroPage.tsx`：天赋区改信息卡渲染，主动槽位增加权重标签展示。
  - `styles.css`：新增天赋信息卡/稀有度徽标/权重标签样式，并提高三列高度与取消槽位内滚动。

## 4. 影响范围

- 影响页面/模块：
  - 英雄页 `技能配置` 标签。
- 是否影响数值平衡：
  - 否。
- 是否影响旧功能：
  - 是，天赋区域 UI 从下拉切换为静态展示卡。

## 5. 验证记录

- 手动验证路径：
  - 英雄页 -> 技能配置：检查天赋卡片内容、稀有度颜色、主动技能权重展示。
  - 切换技能槽位：确认候选去重与权重显示正常。
- 验证结果：
  - 页面渲染与交互正常，构建通过。
- 未覆盖风险：
  - 未做不同分辨率下的视觉回归截图对比。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/04-英雄与队伍模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是（新增本开发记录索引）。
- 是否更新数值指南（如涉及）：
  - 否。

## 7. 回滚说明

- 快速回滚文件：
  - `src/pages/HeroPage.tsx`
  - `src/styles.css`
  - `src/types/battle.ts`
  - `src/data/battleSkills.ts`
- 回滚后需要复查的点：
  - 天赋区是否恢复下拉展示。
  - 主动技能权重标签是否移除。
