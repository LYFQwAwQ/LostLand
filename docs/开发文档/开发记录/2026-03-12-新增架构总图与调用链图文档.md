# 开发记录：新增架构总图与调用链图文档

- 日期：2026-03-12
- 任务：执行“补一份架构总图/调用链图版说明文档”

## 1. 背景

在完成开发文档重构后，仍然缺少一份“图示化总览”文档。后续开发者虽然能看模块文档，但很难第一时间建立全局链路认知。

## 2. 本次改动

### 2.1 新增架构图文档

新增文件：

- `docs/开发文档/08-架构总图与调用链图.md`

内容包含：

1. 启动装配图（`main.tsx` -> Provider -> Router -> App）
2. 路由总图
3. 状态归属总图（4 个 Provider 与页面关系）
4. 核心业务调用链图
5. 战斗结果回写链路图
6. 数据与配置入口图
7. 当前架构限制和按图定位改动入口

### 2.2 同步索引与入口文档

更新：

- `docs/开发文档/README.md`
  - 快速阅读顺序中加入 `08-架构总图与调用链图.md`
  - 文档索引中新增该文档说明
- `docs/开发文档/01-项目与目录总览.md`
  - 在项目定位处补充“先读架构图文档”的入口提示

## 3. 影响面

本次改动仅涉及文档，不改业务代码。

文档层面的收益：

1. 新成员可先看图再看模块文档，降低上手成本。
2. 变更定位由“按目录猜”改为“按链路图定位”。
3. 对 Provider 边界和回写链路的误判风险降低。

## 4. 后续建议

后续如果新增 Provider 或重构主链路（例如引入动态英雄状态源），需要优先更新该架构图文档，再更新各模块文档。
