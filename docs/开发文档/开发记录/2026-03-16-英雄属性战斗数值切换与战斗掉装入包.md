# 2026-03-16-英雄属性战斗数值切换与战斗掉装入包

## 1. 任务信息

- 日期：2026-03-16
- 开发者：Codex
- 任务名称：英雄属性页原始/战斗数值切换 + 战斗掉落装备真实入库
- 需求来源：用户需求（实现 TODO 条目并先完成文档对齐）

## 2. 改动目标

- 本次要解决什么问题：
  - 在英雄属性页新增单按钮，支持“原始数值”与“战斗数值”全量区块切换。
  - 打通战斗掉落装备入包链路，让掉落装备真实进入全局装备背包并参与穿戴。
  - 同步清理 TODO 已完成项，并补充“记忆数值尚未接入战斗”的后续待办。
- 不在本次范围内的内容：
  - 不接入记忆数值效果到战斗引擎（仅补 TODO）。
  - 不调整战斗掉落表权重、地图压制掉落修正或经济系统。
  - 不改 `package.json`、`vite.config.ts`、`tsconfig*` 工程配置。

## 3. 代码改动清单

- 改动文件：
  - `src/pages/HeroPage.tsx`
  - `src/state/EquipmentInventoryProvider.tsx`
  - `src/styles.css`
  - `docs/开发文档/04-英雄与队伍模块.md`
  - `docs/开发文档/05-状态管理与数据模块.md`
  - `docs/开发文档/07-战斗系统模块.md`
  - `docs/开发文档/TODO-项目后续开发.md`
  - `docs/开发文档/README.md`
- 每个文件改了什么（1-2 句）：
  - `HeroPage.tsx`：新增属性页单按钮切换，支持“原始数值/战斗数值”全量展示；战斗数值按 `battleAdapters + battleEngine` 的被动/天赋结算规则计算。
  - `EquipmentInventoryProvider.tsx`：新增战斗掉落装备池状态，`collectBattleDrops` 同时写入材料与装备；`refreshItems` 清掉掉装池并重刷样本；快照导入导出补齐掉装池字段。
  - `styles.css`：补充属性页模式切换区样式及移动端适配。
  - 文档：同步更新模块说明、TODO 完成项与新待办，并登记本次开发记录索引。

## 4. 影响范围

- 影响页面/模块：
  - 英雄页（`/hero/:heroId`）属性页
  - 背包页（`/inventory`）装备列表
  - 战斗页（`/battle/:nodeId`）掉落回写链路
  - `EquipmentInventoryProvider` 快照结构
- 是否影响数值平衡：
  - 否（本次主要是“展示切换 + 掉装入包链路”打通，不改掉落权重和技能数值）。
- 是否影响旧功能：
  - 有：测试期点击“刷新装备样本”会清空战斗掉落装备池；属有意设计。

## 5. 验证记录

- 手动验证路径：
  - 英雄页 -> 基础属性：点击切换按钮，确认核心/战辅/元素/高级区块全部切换。
  - 节点战斗 -> 胜利 -> 打开背包装备页：确认本场掉落装备进入全局列表。
  - 英雄页装备体系：确认可从入包掉装中进行正常穿戴/卸下。
- 验证结果：
  - `npm run build` 通过（`tsc -b && vite build`）。
- 未覆盖风险：
  - 未做自动化测试；记忆数值仍未接入战斗结算（已补 TODO）。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/04-英雄与队伍模块.md`
  - `docs/开发文档/05-状态管理与数据模块.md`
  - `docs/开发文档/07-战斗系统模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是（新增本次开发记录索引）。
- 是否更新数值指南（如涉及）：
  - 否（本次未新增可调参数入口）。

## 7. 回滚说明

- 快速回滚文件：
  - `src/pages/HeroPage.tsx`
  - `src/state/EquipmentInventoryProvider.tsx`
  - `src/styles.css`
  - `docs/开发文档/04-英雄与队伍模块.md`
  - `docs/开发文档/05-状态管理与数据模块.md`
  - `docs/开发文档/07-战斗系统模块.md`
  - `docs/开发文档/TODO-项目后续开发.md`
  - `docs/开发文档/README.md`
  - `docs/开发文档/开发记录/2026-03-16-英雄属性战斗数值切换与战斗掉装入包.md`
- 回滚后需要复查的点：
  - 英雄属性页将退回无战斗数值切换状态。
  - 战斗掉落装备不再进入全局背包，仅保留展示。
