# 2026-03-08-技能配置目录分层重构

## 1. 任务信息

- 日期：2026-03-08
- 开发者：Codex
- 任务名称：战斗技能配置改为目录分层 + 五类技能文件
- 需求来源：用户需求（按通用/职业/怪物层级重组配置）

## 2. 改动目标

- 本次要解决什么问题：
  - 将原单文件技能配置重构为目录结构，便于按来源和技能类别维护。
  - 第一层按 `common/paladin/mage/enemy-normal/enemy-elite/enemy-boss/enemy-common` 分类。
  - 每个目录下按五类主动技能文件拆分（`assault/defend/inspire/afflict/succor`），并保留 `passives/talents`。
- 不在本次范围内的内容：
  - 不改战斗公式与技能数值平衡。
  - 不新增职业或新增怪物原型。

## 3. 代码改动清单

- 改动文件：
  - `src/data/battleSkills/*/*.json`（新增目录与分文件）
  - `src/data/battleSkills.ts`
  - `src/vite-env.d.ts`
  - `docs/开发文档/07-战斗系统模块.md`
  - `docs/开发文档/配置文件修改指南.md`
  - `docs/开发文档/战斗技能配置文件说明-策划.md`
  - `docs/开发文档/README.md`
- 每个文件改了什么（1-2 句）：
  - 新建技能配置目录树并按五类主动技能分文件落盘。
  - `battleSkills.ts` 改为扫描 `battleSkills/*/*.json` 自动聚合并校验，支持 `common/meta.json` 指定保底技能。
  - 新增 `vite-env.d.ts` 引入 `vite/client` 类型，支持 `import.meta.glob`。
  - 文档同步更新为“目录分层配置模式”。

## 4. 影响范围

- 影响页面/模块：
  - 战斗技能加载层（`battleSkills.ts`）
  - 策划改表流程与文档
- 是否影响数值平衡：
  - 否（仅配置组织方式重构，技能数据内容保持一致）
- 是否影响旧功能：
  - 中低风险；若配置文件放错目录或写成非数组，技能会被加载层忽略。

## 5. 验证记录

- 手动验证路径：
  - 检查目录下各分类文件可被加载。
  - 进入战斗查看我方/敌方技能释放是否正常。
- 验证结果：
  - `npm run build` 通过（TypeScript + Vite）。
- 未覆盖风险：
  - 未补自动化测试；后续若新增目录命名规则变化，需同步加载器白名单。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/07-战斗系统模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是
- 是否更新数值指南（如涉及）：
  - 是（战斗技能配置入口改为目录结构）

## 7. 回滚说明

- 快速回滚文件：
  - `src/data/battleSkills.ts`
  - `src/data/battleSkills/`
  - `src/vite-env.d.ts`
- 回滚后需要复查的点：
  - 技能加载入口是否恢复单文件模式。
  - 文档中配置路径是否与实际一致。
