# 开发记录：基地核心与传说装备命名及主线任务

## 1. 任务信息

- 日期：2026-03-15
- 开发者：Codex
- 任务名称：修复传说品质显示、传说装备命名调整、基地核心建筑与主线任务入口
- 需求来源：用户需求（UI 文案与组织系统功能扩展）

## 2. 改动目标

- 本次要解决什么问题：
  - 修复“传说”品质徽章彩色特效下文字不可见的问题。
  - 将背包相关界面中的“传奇装备”文案改为“传说装备”。
  - 新增组织功能建筑“基地核心”，并提供两个子界面：基地状态、主线任务。
  - 主线任务改为固定任务线，不随机；已完成任务置灰并保留在底部。
- 不在本次范围内的内容：
  - 主线任务正式完成条件与奖励结算（当前仍为测试完成按钮）。
  - 基地状态更多维度信息（当前先展示建筑数量统计）。

## 3. 代码改动清单

- 改动文件：
  - `src/styles.css`
  - `src/pages/InventoryPage.tsx`
  - `src/pages/HeroPage.tsx`
  - `src/data/organizationData.ts`
  - `src/state/OrganizationProvider.tsx`
  - `src/pages/OrganizationPage.tsx`
- 每个文件改了什么（1-2 句）：
  - `styles.css`：重做 `quality-legendary-exclusive`，保留彩色流动效果同时确保文字可见；新增基地核心子界面样式。
  - `InventoryPage.tsx` / `HeroPage.tsx`：用户可见文案从“传奇装备”调整为“传说装备”（上限提示、页签与标题文案同步）。
  - `organizationData.ts`：新增唯一功能建筑 `base_core`，并新增固定主线任务定义 `organizationMainQuestDefinitions`。
  - `OrganizationProvider.tsx`：新增组织主线任务状态管理（接取、完成、解锁下一条）。
  - `OrganizationPage.tsx`：基地核心弹窗新增“基地状态/主线任务”按钮切换；实现建筑数量统计与固定主线任务列表（完成项置灰保留底部）。

## 4. 影响范围

- 影响页面/模块：
  - 背包页（传说装备页签文案与品质展示）
  - 英雄页装备选择提示
  - 组织基地页（新增基地核心功能子界面）
  - 组织状态容器（新增主线任务状态）
- 是否影响数值平衡：否（当前主要是 UI 与状态机接入）
- 是否影响旧功能：低风险（任务大厅、建造、扩展、布告栏任务链路保持原逻辑）

## 5. 验证记录

- 手动验证路径：
  - 背包 -> 传说装备页签，查看“传说”品质徽章文字是否可见。
  - 英雄 -> 装备页签，查看传说装备上限提示文案。
  - 组织基地 -> 建造并点击“基地核心”，切换“基地状态/主线任务”子界面。
  - 主线任务：接取 -> 标记完成（测试）-> 验证下一条解锁、已完成任务置灰且保留在底部。
- 验证结果：
  - 构建通过，以上功能均可用。
- 未覆盖风险：
  - 主线任务暂为测试完成按钮，尚未接入正式完成条件与奖励结算。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/03-节点交互模块.md`
  - `docs/开发文档/04-英雄与队伍模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：是
- 是否更新数值指南（如涉及）：是（`配置文件修改指南.md` 增加基地核心主线任务参数入口）

## 7. 回滚说明

- 快速回滚文件：
  - `src/data/organizationData.ts`
  - `src/state/OrganizationProvider.tsx`
  - `src/pages/OrganizationPage.tsx`
  - `src/styles.css`
  - `src/pages/InventoryPage.tsx`
  - `src/pages/HeroPage.tsx`
- 回滚后需要复查的点：
  - 传说品质文字是否再次不可见。
  - 组织建筑“基地核心”与主线任务入口是否移除干净。
