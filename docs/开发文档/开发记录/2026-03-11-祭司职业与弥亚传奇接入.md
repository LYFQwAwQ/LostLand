# 2026-03-11-祭司职业与弥亚传奇接入

## 1. 任务信息

- 日期：2026-03-11
- 开发者：Codex
- 任务名称：新增祭司职业并接入传奇英雄弥亚（Miya）
- 需求来源：用户需求（职业扩展 + 传奇英雄配置与测试接入）

## 2. 改动目标

- 本次要解决什么问题：
  - 新增 `priest` 职业并打通英雄、装备、技能、战斗链路。
  - 按设计文档实现传奇英雄“弥亚（Miya）”并接入立绘。
  - 传奇专属技能与天赋拆分到独立配置文件，不与职业池混放。
- 不在本次范围内的内容：
  - 酒馆招募闭环与传奇正式获取流程。
  - 记忆正式产出链路。

## 3. 代码改动清单

- 改动文件：
  - `src/types/game.ts`
  - `src/types/battle.ts`
  - `src/lib/equipmentCatalog.ts`
  - `src/components/hero/PriestGearBoard.tsx`
  - `src/pages/HeroPage.tsx`
  - `src/pages/InventoryPage.tsx`
  - `src/data/heroMemories.ts`
  - `src/data/battleSkills.ts`
  - `src/data/battleSkills/common/assault.json`
  - `src/data/battleSkills/paladin/talents.json`
  - `src/data/battleSkills/mage/talents.json`
  - `src/data/battleSkills/ranger/talents.json`
  - `src/data/battleSkills/priest/*.json`
  - `src/data/config/heroGeneration.json`
  - `src/data/config/legendaryHeroes.json`
  - `src/data/config/legendaryHeroSkills.json`
  - `src/data/heroRoster.ts`
  - `src/data/battleUnits.ts`
  - `src/lib/battleAdapters.ts`
  - `src/lib/battleEngine.ts`
  - `src/lib/battleLoadoutRules.ts`
  - `src/styles.css`
  - `public/images/heroes/Miya.png`
- 每个文件改了什么（1-2 句）：
  - 扩展 `HeroClass` 到 `priest`，补齐祭司装备槽与展示组件。
  - 新增祭司职业技能池并接入随机英雄生成配置与分职业保底主动。
  - 新建 `legendaryHeroSkills.json`，将传奇专属天赋/技能从职业池迁出。
  - 新增传奇英雄弥亚配置，并临时固定注入初始英雄用于测试。
  - 战斗引擎新增 `randomEnemies/targetCount`、`healAlliesOnKill`，并接入弥亚专属机制。
  - 追加修正：去除英雄页立绘灰度滤镜，恢复 Miya 彩色立绘显示；将 Miya 初始技能收敛为“专属主动 + 基础攻击 / 专属被动”。
  - 追加修正：敌人配置新增 `race` 字段并写入战斗标签（`race:*`），用于种族克制判定。
  - 追加修正：英雄页立绘改为软边界渐隐显示（移除硬边框）；战斗页日志改为“连战会话累计日志”。
  - 追加修正：将立绘软边界进一步收敛为“椭圆 mask 四周渐隐”，避免剩余硬矩形边缘。

## 4. 影响范围

- 影响页面/模块：
  - 英雄页装备体系、技能配置可选池、记忆筛选。
  - 背包页记忆职业统计。
  - 战斗技能加载与战斗结算逻辑。
  - 启动英雄生成（固定 + 随机）。
- 是否影响数值平衡：是（新增职业参数、传奇技能效果、随机职业权重）。
- 是否影响旧功能：否（旧三职业与旧传奇仍可正常使用，构建通过）。

## 5. 验证记录

- 手动验证路径：
  - `npm run build`
  - 英雄页查看弥亚与祭司装备栏布局。
  - 技能配置页确认祭司仅出现已学技能。
- 验证结果：
  - 构建通过（`tsc -b && vite build`）。
  - 祭司职业、弥亚配置、传奇独立技能文件均成功接入。
- 未覆盖风险：
  - 弥亚“亡灵双倍”依赖敌方 `tags` 中包含 `undead`，当前默认敌池未大量标注该标签。
  - “万物共鸣”现为可用实现，后续可抽象为通用受击触发框架。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/04-英雄与队伍模块.md`
  - `docs/开发文档/07-战斗系统模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：是。
- 是否更新数值指南（如涉及）：是（新增 `legendaryHeroSkills` 配置说明与战斗字段）。

## 7. 回滚说明

- 快速回滚文件：
  - `src/data/config/legendaryHeroes.json`
  - `src/data/config/legendaryHeroSkills.json`
  - `src/data/heroRoster.ts`
  - `src/data/battleSkills/priest/*`
  - `src/lib/battleEngine.ts`
- 回滚后需要复查的点：
  - `HeroClass` 扩展是否同步回退。
  - 英雄页/背包页是否仍引用 `priest` 分支。
  - 构建是否恢复通过。
