# 2026-03-08-随机技能数量区间化与测试去重待移除

## 1. 任务信息

- 日期：2026-03-08
- 开发者：Codex
- 任务名称：随机英雄技能数量按规则随机 + 测试去重标记为后续移除
- 需求来源：用户反馈

## 2. 改动目标

- 本次要解决什么问题：
  - 将普通随机英雄“初始技能数量”从固定值改为可配置随机区间，符合百科“按抽取规则抽取技能”的描述。
  - 将“同次启动同职业天赋优先去重”明确为测试期逻辑，并加入项目 TODO，正式版需移除。
- 不在本次范围内的内容：
  - 技能获取正式来源（任务/训练/消耗）与职业成长解锁流程。

## 3. 代码改动清单

- 改动文件：
  - `src/data/heroRoster.ts`
  - `src/data/config/heroGeneration.json`
  - `docs/开发文档/TODO-项目后续开发.md`
  - `docs/开发文档/04-英雄与队伍模块.md`
  - `docs/开发文档/配置文件修改指南.md`
- 每个文件改了什么（1-2 句）：
  - `heroRoster.ts`：新增 `active/passive` 抽取区间解析与随机抽样逻辑；保持对固定数量字段的兼容。
  - `heroGeneration.json`：普通随机英雄初始技能改为区间配置（`4~6` 主动、`1~2` 被动），并保留 `basic_attack` 必带配置。
  - TODO 新增“移除同次启动同职业天赋去重测试逻辑”事项。
  - 模块文档与配置指南同步更新为区间字段语义与当前默认值。

## 4. 影响范围

- 影响页面/模块：
  - 启动英雄生成（随机普通英雄初始技能列表）
  - 英雄页技能配置候选（因 `learnedSkills` 结果变化）
- 是否影响数值平衡：
  - 是。普通随机英雄开局技能覆盖率改为区间浮动。
- 是否影响旧功能：
  - 会改变此前“固定数量”的生成结果；传奇英雄与固定测试英雄不受影响。

## 5. 验证记录

- 手动验证路径：
  - 多次刷新启动，观察普通随机英雄已学主动/被动技能数在配置区间内波动。
  - 检查技能配置页候选是否仍严格来自该英雄 `learnedSkills`。
- 验证结果：
  - 构建通过，逻辑生效。
- 未覆盖风险：
  - 尚未做长样本统计验证各职业抽样分布。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/04-英雄与队伍模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是
- 是否更新数值指南（如涉及）：
  - 是

## 7. 回滚说明

- 快速回滚文件：
  - `src/data/heroRoster.ts`
  - `src/data/config/heroGeneration.json`
  - `docs/开发文档/TODO-项目后续开发.md`
- 回滚后需要复查的点：
  - 回滚到固定数量后，需确认文档和配置字段描述与代码行为一致。
