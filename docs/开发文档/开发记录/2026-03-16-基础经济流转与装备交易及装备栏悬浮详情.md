# 2026-03-16 开发记录：基础经济流转与装备交易及装备栏悬浮详情

## 1. 任务信息

- 日期：2026-03-16
- 开发者：Codex
- 任务名称：基础经济流转、布告栏赏金/声望结算、英雄装备栏已穿戴槽位悬浮详情
- 需求来源：项目 TODO + 需求确认对话

## 2. 改动目标

- 本次要解决什么问题：
  - 将主界面静态金币展示改为真实全局经济状态。
  - 在 ST1（shop）与 ST2（market）节点接通基础装备“买入/卖出”闭环。
  - 将布告栏任务奖励中的赏金/声望接入正式状态并结算。
  - 在英雄装备栏界面支持“悬停已装备槽位显示半透明属性弹窗”。
- 不在本次范围内的内容：
  - 商店/商铺材料与消耗品交易、大宗贸易完整逻辑。
  - 声望玩法效果（当前仅做数值展示与结算）。

## 3. 代码改动清单

- 改动文件：
  - `src/data/config/economyConfig.ts`
  - `src/state/EquipmentInventoryProvider.tsx`
  - `src/components/layout/MainLayout.tsx`
  - `src/pages/NodeActionPage.tsx`
  - `src/components/hero/PaladinGearBoard.tsx`
  - `src/components/hero/MageGearBoard.tsx`
  - `src/components/hero/RangerGearBoard.tsx`
  - `src/components/hero/PriestGearBoard.tsx`
  - `src/pages/HeroPage.tsx`
  - `src/lib/mapRules.ts`
  - `src/styles.css`
- 每个文件改了什么（1-2 句）：
  - `economyConfig.ts`：新增经济初始值与装备买卖价格配置，并提供买入/卖出价格计算函数。
  - `EquipmentInventoryProvider.tsx`：新增金币/声望状态、买卖接口、卖出过滤状态；`grantMissionRewards` 结算赏金/声望；快照支持经济与卖出状态。
  - `MainLayout.tsx`：资源面板金币/声望改为读取 Provider 真实状态。
  - `NodeActionPage.tsx`：在 `shop` 与 `market` 接入基础装备交易 UI 和买卖行为；保留静态表作为内容预览；布告栏奖励仍走同一结算链路。
  - 四个 `*GearBoard.tsx`：新增 `onHoverSlot` 悬浮回调，支持槽位鼠标进入/移动/离开事件。
  - `HeroPage.tsx`：`HeroGearContent` 增加“已装备槽位悬浮详情”浮层，复用现有装备详情样式。
  - `mapRules.ts`：更新 `shop/market` 动作描述，标记为已接通基础装备买卖。
  - `styles.css`：新增节点交易区块样式（双栏、列表、反馈、价格等）。

## 4. 影响范围

- 影响页面/模块：
  - 主布局资源区（金币/声望）
  - 节点交互页（ST1/ST2 交易）
  - 英雄页装备页签（已装备槽位悬浮弹窗）
  - 背包与战斗相关的经济/装备状态链路
- 是否影响数值平衡：是（新增装备买卖价格曲线与初始金币）。
- 是否影响旧功能：否（保留原静态表展示，不影响原有占位数据浏览；战斗掉落入包与装备穿戴链路保持兼容）。

## 5. 验证记录

- 手动验证路径：
  - 世界地图 -> ST1 `shop` / ST2 `market`：买入装备、卖出未穿戴装备、尝试卖出已穿戴装备与传说装备。
  - 世界地图 -> 节点 -> 布告栏：提交任务后检查金币/声望变化。
  - 英雄页 -> 装备页签：鼠标悬浮已装备槽位，检查是否出现详情浮层。
  - 主界面左侧资源区：观察金币/声望随交易与任务结算变化。
- 验证结果：代码级联动已接通，需通过构建与页面联调确认最终表现。
- 未覆盖风险：
  - 旧预设快照载入后经济字段缺失时会回退到默认初始值。
  - 商店当前仅装备交易，材料/消耗品交易仍待后续扩展。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/03-节点交互模块.md`
  - `docs/开发文档/04-英雄与队伍模块.md`
  - `docs/开发文档/05-状态管理与数据模块.md`
  - `docs/开发文档/配置文件修改指南.md`
  - `docs/开发文档/TODO-项目后续开发.md`
- 是否更新索引（`docs/开发文档/README.md`）：是
- 是否更新数值指南（如涉及）：是（新增 `economyConfig.ts` 参数说明）

## 7. 回滚说明

- 快速回滚文件：
  - `src/data/config/economyConfig.ts`
  - `src/state/EquipmentInventoryProvider.tsx`
  - `src/pages/NodeActionPage.tsx`
  - `src/components/layout/MainLayout.tsx`
  - `src/pages/HeroPage.tsx`
  - `src/components/hero/*GearBoard.tsx`
  - `src/styles.css`
- 回滚后需要复查的点：
  - 资源面板是否回到静态展示。
  - 布告栏奖励是否仍只发放材料/消耗品。
  - 英雄装备槽位悬浮是否仅保留装备选择弹窗内的详情提示。
