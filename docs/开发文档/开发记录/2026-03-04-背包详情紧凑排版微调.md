# 2026-03-04-背包详情紧凑排版微调

## 1. 任务信息

- 日期：2026-03-04
- 开发者：Codex
- 任务名称：背包详情紧凑排版微调
- 需求来源：用户反馈（详情信息过于分散，品质展示语义优化）

## 2. 改动目标

- 本次要解决什么问题：
  - 同模块内属性项取消分割线，减少视觉割裂。
  - 品质改为与“具体类型”同行展示，仅显示名称（如“传说”），不显示“品质：”和颜色字。
- 不在本次范围内的内容：
  - 不改动装备生成逻辑和筛选逻辑。

## 3. 代码改动清单

- 改动文件：
  - `src/pages/InventoryPage.tsx`
  - `src/styles.css`
- 每个文件改了什么（1-2 句）：
  - `src/pages/InventoryPage.tsx`：将品质从标题下元信息行移到具体类型旁边显示（名称制）。
  - `src/styles.css`：新增 `inventory-subtype-line` 样式，并移除属性列表行内虚线分割。

## 4. 影响范围

- 影响页面/模块：
  - 背包页装备详情弹窗。
- 是否影响数值平衡：
  - 否（纯 UI 展示微调）。
- 是否影响旧功能：
  - 否，详情内容与交互保持一致。

## 5. 验证记录

- 手动验证路径：
  - 进入 `背包 -> 装备`，打开任意装备详情。
  - 检查“具体类型”右侧是否直接显示品质名称（如“传说”）。
  - 检查基础属性/附加词条列表内部不再有逐行分割线。
- 验证结果：
  - 构建通过：`npm run build`。
  - 单测通过：`node --experimental-strip-types --test --test-isolation=none tests/equipmentSystem.test.ts`（7/7）。
- 未覆盖风险：
  - 暂无新增风险。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/06-UI与样式模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是
- 是否更新数值指南（如涉及）：
  - 否

## 7. 回滚说明

- 快速回滚文件：
  - `src/pages/InventoryPage.tsx`
  - `src/styles.css`
- 回滚后需要复查的点：
  - 详情中品质显示位置是否恢复；
  - 属性列表视觉密度是否回退为旧样式。
