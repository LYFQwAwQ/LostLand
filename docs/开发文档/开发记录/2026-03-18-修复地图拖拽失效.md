# 2026-03-18-修复地图拖拽失效

## 1. 任务信息

- 日期：2026-03-18
- 开发者：Codex
- 任务名称：修复地图拖拽失效
- 需求来源：用户反馈“地图并没有办法拖动”。

## 2. 改动目标

- 本次要解决什么问题：
  - 修复世界地图在地区首次加载后无法拖拽的问题。
  - 提高地图拖拽在低兼容环境下的尺寸监听稳定性。
- 不在本次范围内的内容：
  - 不调整地图节点、连线、地区拓扑和月结算数值。
  - 不改动其他页面交互逻辑。

## 3. 代码改动清单

- `src/pages/WorldMapPage.tsx`
  - 地图尺寸监听从仅首次挂载执行，调整为依赖 `region?.id` 重新绑定，保证地区加载后能拿到真实 `map-stage` 尺寸。
  - 增加 `window.resize` 监听，并在 `ResizeObserver` 不可用时使用该监听作为兜底，避免 `stageSize` 卡在 `0`。
- `docs/开发文档/02-地图生成与月度结算模块.md`
  - 补充拖拽能力的实现注意事项，明确 `stageSize=0` 会导致 `panOffset` 被钳制、表现为不可拖动。
- `docs/开发文档/README.md`
  - 新增本开发记录索引。

## 4. 影响范围

- 影响页面/模块：
  - 世界地图页面（`WorldMapPage`）拖拽浏览与回中交互。
- 是否影响数值平衡：
  - 否。
- 是否影响旧功能：
  - 否（仅修复拖拽交互链路，不改业务数据）。

## 5. 验证记录

- 手动验证路径：
  - `npm run build`
- 验证结果：
  - 构建通过，类型检查与打包通过。
- 未覆盖风险：
  - 本次未在真机触屏环境做手动拖拽验证；如需可补充移动端手测。

## 6. 文档更新记录

- 已更新的模块文档：
  - `docs/开发文档/02-地图生成与月度结算模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是。
- 是否更新数值指南（如涉及）：
  - 否（无新增可调参数）。

## 7. 回滚说明

- 快速回滚文件：
  - `src/pages/WorldMapPage.tsx`
- 回滚后需要复查的点：
  - 世界地图进入任意地区后，拖拽是否再次失效。
  - `map-center-btn` 回中与拖拽状态样式是否保持正常。
