# 2026-03-06-战斗系统实时模板接入

## 1. 任务信息

- 日期：2026-03-06
- 开发者：Codex
- 任务名称：战斗系统实时模板接入
- 需求来源：用户需求（战斗系统正式模板落地）

## 2. 改动目标

- 本次要解决什么问题：
  - 将 BL 节点“讨伐”从占位入口升级为可运行的实时战斗系统模板；
  - 支持双方 1-6 人、前后排结构、完整技能槽位模型（1 天赋 + 10 主动 + 10 被动）；
  - 接入战斗日志、状态结算、胜负判定与掉落展示。
- 不在本次范围内的内容：
  - 队伍拖拽编成编辑器；
  - 完整怪物掉落表/保底系统；
  - 全量职业技能与怪物库平衡。

## 3. 代码改动清单

- 改动文件：
  - `src/types/battle.ts`
  - `src/data/battleSkills.ts`
  - `src/data/battleUnits.ts`
  - `src/lib/battleAdapters.ts`
  - `src/lib/battleEngine.ts`
  - `src/pages/BattlePage.tsx`
  - `src/App.tsx`
  - `src/pages/NodeActionPage.tsx`
  - `src/lib/mapRules.ts`
  - `src/styles.css`
- 每个文件改了什么（1-2 句）：
  - 新增 battle 类型系统、技能/单位数据、适配器与实时战斗引擎；
  - 新增战斗页面并接入路由 `/battle/:nodeId`；
  - 节点 battle 入口改为跳转实时战斗；
  - 增加战斗相关样式与日志/掉落展示；
  - 战斗掉落暂时复用装备生成器，并在引擎内保留 TODO 注释。

## 4. 影响范围

- 影响页面/模块：
  - 节点交互页（battle 分支）
  - 新增实时战斗页
  - 战斗数据与算法模块
- 是否影响数值平衡：
  - 是（新增战斗参数与技能参数，当前为首版可跑默认值）。
- 是否影响旧功能：
  - 否（地图、英雄、背包等旧链路可继续使用）。

## 5. 验证记录

- 手动验证路径：
  - 世界地图 -> 进入 BL 节点 -> 进入讨伐 -> 进入实时战斗；
  - 点击开始/暂停、调整倍速、重置本场、重新侦察；
  - 观察行动条、日志、状态、胜负与掉落显示。
- 验证结果：
  - 通过 `npm run build`（TS 类型检查 + Vite 构建通过）。
- 未覆盖风险：
  - 技能与怪物数值尚未做平衡校准；
  - 掉落仍为临时复用逻辑，未接怪物掉落表。

## 6. 文档更新记录

- 已更新的模块文档：
  - `03-节点交互模块.md`
  - 新增 `07-战斗系统模块.md`
- 是否更新索引（`docs/开发文档/README.md`）：
  - 是
- 是否更新数值指南（如涉及）：
  - 是（新增战斗参数词典）

## 7. 回滚说明

- 快速回滚文件：
  - `src/pages/BattlePage.tsx`
  - `src/lib/battleEngine.ts`
  - `src/data/battleSkills.ts`
  - `src/data/battleUnits.ts`
  - `src/lib/battleAdapters.ts`
  - `src/App.tsx`（移除路由）
  - `src/pages/NodeActionPage.tsx`（恢复占位）
- 回滚后需要复查的点：
  - 节点 battle 入口是否仍指向可用页面；
  - 文档索引是否与实际代码状态一致。

